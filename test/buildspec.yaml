version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo Running tests...
      - npm test

artifacts:
  files:
    - '**/*'
  discard-paths: yes
